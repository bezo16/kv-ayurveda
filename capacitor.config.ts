import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'yogapit.ayurveda',
  appName: 'yogapit-ayurveda',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
